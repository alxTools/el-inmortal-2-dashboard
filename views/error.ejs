<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/dashboard.css">
</head>
<body>
    <%- include('partials/navbar') %>

    <div class="container" style="padding-top: 50px;">
        <div class="section" style="text-align: center; max-width: 600px; margin: 0 auto;">
            <h1 style="font-size: 4em; margin-bottom: 20px;">⚠️</h1>
            <h2 style="color: #ffd700; margin-bottom: 20px;"><%= title %></h2>
            <p style="font-size: 1.2em; color: #888; margin-bottom: 30px;"><%= message %></p>
            
            <% if (Object.keys(error).length > 0 && process.env.NODE_ENV === 'development') { %>
                <div style="background: rgba(220, 53, 69, 0.1); border: 1px solid rgba(220, 53, 69, 0.3); padding: 20px; border-radius: 10px; text-align: left; margin-top: 30px;">
                    <h3 style="color: #dc3545; margin-bottom: 10px;">Detalles del Error (Debug):</h3>
                    <pre style="color: #888; overflow-x: auto;"><%= JSON.stringify(error, null, 2) %></pre>
                </div>
            <% } %>
            
            <a href="/" class="btn" style="margin-top: 30px;">← Volver al Dashboard</a>
        </div>
    </div>

    <%- include('partials/footer') %>
</body>
</html>