<!-- Track Detail Modal -->
<div id="track-modal" class="track-modal" style="display:none;">
    <div class="track-modal-overlay" onclick="closeTrackModal()"></div>
    
    <div class="track-modal-content">
        <button class="track-modal-close" onclick="closeTrackModal()" aria-label="Cerrar">
            &times;
        </button>
        
        <div class="track-modal-header">
            <img id="modal-track-cover" src="" alt="Cover" class="track-modal-cover">
            <div class="track-modal-info">
                <h2 id="modal-track-title"></h2>
                <p id="modal-track-producer" class="track-meta"></p>
                <p id="modal-track-features" class="track-meta"></p>
                <p id="modal-track-duration" class="track-meta"></p>
            </div>
        </div>
        
        <div class="track-modal-body">
            <!-- Audio Player Section -->
            <div class="audio-section">
                <h3>🎵 Preview</h3>
                <div class="loop-selector">
                    <button class="loop-btn active" data-loop="1" onclick="selectLoop(1)">Loop 1</button>
                    <button class="loop-btn" data-loop="2" onclick="selectLoop(2)">Loop 2</button>
                    <button class="loop-btn" data-loop="3" onclick="selectLoop(3)">Loop 3</button>
                    <button class="loop-btn" data-loop="4" onclick="selectLoop(4)">Loop 4</button>
                </div>
                
                <audio id="track-audio-player" controls>
                    <source src="" type="audio/wav">
                    Tu navegador no soporta audio HTML5.
                </audio>
            </div>
            
            <!-- Promo Generator Section -->
            <div class="promo-section">
                <h3>📸 Crea tu Promo</h3>
                <p class="promo-description">
                    Sube tu foto y crea un post promocional único para compartir en redes sociales.
                </p>
                
                <div class="promo-upload">
                    <input type="file" id="promo-photo-input" accept="image/*" style="display:none;" onchange="handlePhotoUpload(event)">
                    <div class="upload-zone" onclick="document.getElementById('promo-photo-input').click()">
                        <span class="upload-icon">📤</span>
                        <p>Haz clic o arrastra tu foto aquí</p>
                        <span class="upload-hint">JPG, PNG - Máximo 5MB</span>
                    </div>
                </div>
                
                <div class="template-selector" style="display:none;" id="template-section">
                    <h4>Elige un diseño:</h4>
                    <div class="templates"
                        <button class="template-btn active" data-template="1" onclick="selectTemplate(1)"
                            <span class="template-preview">Esquina</span>
                            <span class="template-desc">Foto en esquina inferior</span>
                        </button>
                        <button class="template-btn" data-template="2" onclick="selectTemplate(2)"
                            <span class="template-preview">Fondo Blur</span>
                            <span class="template-desc">Foto como fondo difuminado</span>
                        </button>
                        <button class="template-btn" data-template="3" onclick="selectTemplate(3)"
                            <span class="template-preview">Split</span>
                            <span class="template-desc">Mitad foto, mitad cover</span>
                        </button>
                        <button class="template-btn" data-template="4" onclick="selectTemplate(4)"
                            <span class="template-preview">Círculo</span>
                            <span class="template-desc">Foto circular sobre cover</span>
                        </button>
                    </div>
                    
                    <div class="promo-preview">
                        <canvas id="promo-canvas" width="1080" height="1080"></canvas>
                    </div>
                    
                    <button class="btn-download-promo" onclick="downloadPromo()">
                        📥 Descargar Promo
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.track-modal {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    z-index: 1000;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px;
}

.track-modal-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.85);
    backdrop-filter: blur(4px);
}

.track-modal-content {
    position: relative;
    background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
    border-radius: 20px;
    max-width: 600px;
    width: 100%;
    max-height: 90vh;
    overflow-y: auto;
    border: 1px solid rgba(250, 204, 21, 0.3);
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.5);
}

.track-modal-close {
    position: absolute;
    top: 15px;
    right: 15px;
    background: rgba(255, 255, 255, 0.1);
    border: none;
    color: #fff;
    font-size: 28px;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.2s;
    z-index: 10;
}

.track-modal-close:hover {
    background: rgba(255, 255, 255, 0.2);
    transform: scale(1.1);
}

.track-modal-header {
    display: flex;
    gap: 20px;
    padding: 30px;
    background: linear-gradient(135deg, rgba(250, 204, 21, 0.1) 0%, transparent 100%);
    border-bottom: 1px solid rgba(250, 204, 21, 0.2);
}

.track-modal-cover {
    width: 150px;
    height: 150px;
    object-fit: cover;
    border-radius: 12px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
}

.track-modal-info {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
}

.track-modal-info h2 {
    color: #facc15;
    font-size: 24px;
    margin: 0 0 10px 0;
}

.track-meta {
    color: #94a3b8;
    font-size: 14px;
    margin: 4px 0;
}

.track-modal-body {
    padding: 30px;
}

.audio-section {
    margin-bottom: 30px;
}

.audio-section h3 {
    color: #facc15;
    font-size: 18px;
    margin-bottom: 15px;
}

.loop-selector {
    display: flex;
    gap: 10px;
    margin-bottom: 20px;
    flex-wrap: wrap;
}

.loop-btn {
    padding: 10px 20px;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(250, 204, 21, 0.3);
    color: #94a3b8;
    border-radius: 25px;
    cursor: pointer;
    transition: all 0.2s;
    font-size: 14px;
}

.loop-btn:hover {
    background: rgba(250, 204, 21, 0.1);
}

.loop-btn.active {
    background: linear-gradient(135deg, #facc15, #fbbf24);
    color: #0f172a;
    border-color: #facc15;
    font-weight: bold;
}

#track-audio-player {
    width: 100%;
    margin-top: 15px;
}

.promo-section {
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    padding-top: 30px;
}

.promo-section h3 {
    color: #facc15;
    font-size: 18px;
    margin-bottom: 10px;
}

.promo-description {
    color: #94a3b8;
    font-size: 14px;
    margin-bottom: 20px;
}

.upload-zone {
    border: 2px dashed rgba(250, 204, 21, 0.3);
    border-radius: 12px;
    padding: 40px;
    text-align: center;
    cursor: pointer;
    transition: all 0.2s;
    background: rgba(255, 255, 255, 0.02);
}

.upload-zone:hover {
    border-color: rgba(250, 204, 21, 0.6);
    background: rgba(250, 204, 21, 0.05);
}

.upload-icon {
    font-size: 48px;
    display: block;
    margin-bottom: 10px;
}

.upload-zone p {
    color: #e2e8f0;
    margin: 0 0 5px 0;
    font-size: 16px;
}

.upload-hint {
    color: #64748b;
    font-size: 12px;
}

.template-selector {
    margin-top: 25px;
}

.template-selector h4 {
    color: #e2e8f0;
    font-size: 16px;
    margin-bottom: 15px;
}

.templates {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 10px;
    margin-bottom: 20px;
}

.template-btn {
    padding: 15px;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(250, 204, 21, 0.2);
    border-radius: 10px;
    cursor: pointer;
    transition: all 0.2s;
    text-align: left;
}

.template-btn:hover {
    border-color: rgba(250, 204, 21, 0.5);
    background: rgba(250, 204, 21, 0.05);
}

.template-btn.active {
    border-color: #facc15;
    background: rgba(250, 204, 21, 0.1);
}

.template-preview {
    display: block;
    color: #facc15;
    font-weight: bold;
    font-size: 14px;
    margin-bottom: 4px;
}

.template-desc {
    display: block;
    color: #94a3b8;
    font-size: 12px;
}

.promo-preview {
    background: rgba(0, 0, 0, 0.3);
    border-radius: 12px;
    padding: 20px;
    margin-bottom: 20px;
    display: flex;
    justify-content: center;
}

#promo-canvas {
    max-width: 100%;
    max-height: 300px;
    border-radius: 8px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
}

.btn-download-promo {
    width: 100%;
    padding: 15px;
    background: linear-gradient(135deg, #facc15, #fbbf24);
    border: none;
    border-radius: 10px;
    color: #0f172a;
    font-weight: bold;
    font-size: 16px;
    cursor: pointer;
    transition: all 0.2s;
}

.btn-download-promo:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 20px rgba(250, 204, 21, 0.3);
}

@media (max-width: 640px) {
    .track-modal-header {
        flex-direction: column;
        align-items: center;
        text-align: center;
    }
    
    .track-modal-cover {
        width: 120px;
        height: 120px;
    }
    
    .templates {
        grid-template-columns: 1fr;
    }
}
</style>
