<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/dashboard.css">
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container" style="padding-top: 40px; max-width: 700px;">
        <h1 style="color: #ffd700; margin-bottom: 30px;">✏️ Editar Productor: <%= producer.name %></h1>

        <div class="section">
            <form action="/producers/<%= producer.id %>?_method=PUT" method="POST">
                <div class="form-row">
                    <div class="form-group">
                        <label>Nombre Artístico</label>
                        <input type="text" name="name" value="<%= producer.name %>" required>
                    </div>
                    <div class="form-group">
                        <label>Nombre Legal</label>
                        <input type="text" name="legal_name" value="<%= producer.legal_name %>">
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label>Email</label>
                        <input type="email" name="email" value="<%= producer.email %>" required>
                    </div>
                    <div class="form-group">
                        <label>Teléfono</label>
                        <input type="tel" name="phone" value="<%= producer.phone %>">
                    </div>
                </div>

                <div class="form-group">
                    <label>Dirección</label>
                    <textarea name="address" rows="2"><%= producer.address %></textarea>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label>Splits Estándar</label>
                        <input type="text" name="split_percentage" value="<%= producer.split_percentage %>">
                    </div>
                    <div class="form-group">
                        <label>Estado</label>
                        <select name="status">
                            <option value="active" <%= producer.status === 'active' ? 'selected' : '' %>>Activo</option>
                            <option value="inactive" <%= producer.status === 'inactive' ? 'selected' : '' %>>Inactivo</option>
                        </select>
                    </div>
                </div>

                <div style="display: flex; gap: 15px; margin-top: 30px;">
                    <button type="submit" class="btn">💾 Actualizar Productor</button>
                    <a href="/producers" class="btn btn-secondary">Cancelar</a>
                </div>
            </form>
        </div>
    </div>

    <%- include('../partials/footer') %>
</body>
</html>